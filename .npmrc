# Skip optional deps (e.g. sharp) and package install scripts (husky, tshy, etc.)
# that hang Vercel's build machine. prisma generate runs in the build command instead.
omit=optional
ignore-scripts=true
