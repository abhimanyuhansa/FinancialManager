# Force public npm registry — prevents SAP internal Artifactory from being used
# (the global ~/.npmrc sets SAP registry which breaks Vercel builds)
registry=https://registry.npmjs.org/
ignore-scripts=true
